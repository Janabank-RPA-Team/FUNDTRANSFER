MERGE INTO FT L
USING (SELECT DISTINCT(ACCOUNT_NO), FT_AMT_MATCHING 
    FROM FT_BC_PARTNER D) TMP
ON (L.ACCOUNT_NUMBER = TMP.ACCOUNT_NO AND TMP.FT_AMT_MATCHING!='0')
WHEN MATCHED THEN 
UPDATE SET L.FT_STATUS='REJECTED_FT';
COMMIT;