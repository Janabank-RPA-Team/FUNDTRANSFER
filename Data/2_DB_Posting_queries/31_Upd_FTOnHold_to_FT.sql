MERGE INTO FT L
USING (SELECT DISTINCT(D.ACCOUNT_NUMBER),D.WRONG_AL, D.CORRECT_AL
FROM FT_FT_ON_HOLD D) TMP
ON (L.ACCOUNT_NUMBER = TMP.ACCOUNT_NUMBER)
WHEN MATCHED THEN
UPDATE SET L.WRONG_AL =TMP.WRONG_AL,
L.CORRECT_AL =TMP.CORRECT_AL,
L.FT_STATUS='HOLD';
commit;