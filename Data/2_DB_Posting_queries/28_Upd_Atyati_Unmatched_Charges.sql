MERGE INTO FT L
USING (SELECT DISTINCT(LOAN_ACCOUNT_NUMBER), FT_AMOUNT_MATCHING 
    FROM FT_ATYATI D) TMP
ON (L.ACCOUNT_NUMBER = TMP.LOAN_ACCOUNT_NUMBER AND TMP.FT_AMOUNT_MATCHING!='0')
WHEN MATCHED THEN 
UPDATE SET L.FT_STATUS='REJECTED_FT';
COMMIT;