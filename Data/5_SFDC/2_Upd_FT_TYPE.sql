UPDATE FT SET FT_TYPE=CASE
WHEN REGEXP_LIKE(IFSC_CODE,'[JSFB]') THEN 'INTERNAL_FT'
ELSE 'EXTERNAL_FT'
END
WHERE ACCOUNT_NUMBER IS NOT NULL OR IFSC_CODE IS NOT NULL AND FT_STATUS != 'HOLD';
commit;