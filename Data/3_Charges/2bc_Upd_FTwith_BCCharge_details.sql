MERGE INTO FT dest
USING (SELECT LOAN_ACCOUNT_NO, FT_AMT_MATCHING FROM FT_COMPUTE_BC)src
ON (dest.ACCOUNT_NUMBER=src.LOAN_ACCOUNT_NO AND dest.CATEGORY='BC_PARTNER' AND dest.FT_STATUS='APPROVED_FT')
WHEN MATCHED THEN  
UPDATE SET dest.FT_AMOUNT_MATCHING=src.FT_AMT_MATCHING;
COMMIT;
