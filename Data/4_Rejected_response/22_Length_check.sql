UPDATE FT_REJECTED_FT 
SET LENGTH_CHECK=
CASE WHEN LENGTH(IFSC_CODE)=11 AND LENGTH(ACCOUNT_NUMBER) IN ('14','16') AND LENGTH(URN_NUMBER)=16 
THEN 'NO'
ELSE 'YES'
END
WHERE URN_NUMBER IS NOT NULL OR IFSC_CODE IS NOT NULL OR ACCOUNT_NUMBER IS NOT NULL;
commit;