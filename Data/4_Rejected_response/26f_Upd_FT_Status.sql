MERGE INTO FT L
USING (SELECT DISTINCT(D.ACCOUNT_NUMBER), D.FT_STATUS
FROM FT_REJECTED_FT D) TMP
ON (L.ACCOUNT_NUMBER = TMP.ACCOUNT_NUMBER AND L.CATEGORY IN('ATYATI','BC_PARTNER'))
WHEN MATCHED THEN
UPDATE SET L.FT_STATUS = TMP.FT_STATUS;
COMMIT;